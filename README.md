# open-forward-NN [ofnn]
An open (accessible parameters), feed-forward-only NN (neural net) library to use it in an dynamic and free way in evolutionary algorithms.

If you need backpropagation learning, it should be possible to add it without a lot of problems. (create a fork)

## Usage
Take a look at the examples.
